import { MontantNegatif } from './montant-negatif';

describe('MontantNegatif', () => {
  it('should create an instance', () => {
    const directive = new MontantNegatif();
    expect(directive).toBeTruthy();
  });
});
